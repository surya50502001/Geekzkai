# TODO: Fix Login Connection and Add Login Endpoint

- [x] Update Program.cs to add JWT authentication services
- [x] Add [HttpPost("login")] method in UserController.cs to handle login, verify credentials, and return a JWT token
- [x] Update AuthContext.jsx to use http://localhost:5131/api as the base URL
- [x] Test the backend and frontend login functionality
